Bitwarden clients, but with the https enforcement removed

Note to self: build with `npm ci`, `cd apps/browser`, `npm run build:prod:chrome`, `vivaldi --pack-extension=$PWD/build --pack-extension-key=...`